{% set name = "LoadsKernel" %}
{% set version = "2024.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/DLR-AE/LoadsKernel/archive/refs/tags/2024.05.tar.gz
  sha256: 3a4cd4c881bf071968f4240b02a013e4a9118fa29d6709d82d13ac01d5aa4a4a
  # generated with curl -sL <url/to/*.tar.gz> | openssl sha256
  
build:
  entry_points:
    - loads-kernel=loadskernel.program_flow:command_line_interface
    - model-viewer=modelviewer.view:command_line_interface
    - loads-compare=loadscompare.compare:command_line_interface
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 1

requirements:
  host:
    - python >=3.10
    - pip
  run:
    - python >=3.10
    - panelaero
    - matplotlib-base
    - numpy
    - scipy
    - psutil
    - h5py
    - pytables
    - pyyaml
    - pandas

test:
  imports:
    - loadscompare
    - loadskernel
    - modelviewer
  commands:
    - pip check
    - loads-kernel --help
  requires:
    - pip

outputs:
  - name: loadskernel

  - name: loadskernel-extras
    requirements:
      host:
        - python >=3.10
      run:
        - python >=3.10
        - mpi4py
        - mayavi
        - traits
        - traitsui
        - pyface
        - jupyter
        - {{ pin_subpackage('loadskernel', exact=True) }}
    test:
      imports:
        - loadskernel

about:
  home: https://github.com/DLR-AE/LoadsKernel
  summary: The Loads Kernel Software allows for the calculation of aircraft loads.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ArneVoss
